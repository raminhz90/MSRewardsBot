<Page x:Class="MicrosoftRewardsBot.Views.AccountManage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MicrosoftRewardsBot.Views" xmlns:viewmodels="clr-namespace:MicrosoftRewardsBot.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:UserVM}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AccountManage">

    <Grid>
        <DataGrid ItemsSource="{Binding AccountList}"  ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}" CellStyle="{StaticResource DataGridCellStyle}"
                  RowStyle="{StaticResource DataGridRowStyle}" >
           
            
        </DataGrid>
        
    </Grid>
</Page>
