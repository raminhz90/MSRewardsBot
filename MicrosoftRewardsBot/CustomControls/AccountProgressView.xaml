<UserControl x:Class="MicrosoftRewardsBot.CustomControls.AccountProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MicrosoftRewardsBot.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance local:AccountProgressView}">
    <StackPanel>
        <ProgressBar  Height="20" Value="{Binding ProgressValue,RelativeSource={RelativeSource AncestorType=local:AccountProgressView}}"/>
        <TextBlock Text="{Binding LogText,RelativeSource={RelativeSource AncestorType=local:AccountProgressView}}"/>


    </StackPanel>
</UserControl>
